using UnityEngine;

namespace Tag.NutSort {
    [CreateAssetMenu(fileName = "PlayerLevel", menuName = Constant.GAME_NAME + "/Currency/PlayerLevel")]
    public class PlayerLevel : Currency
    {
    }
}
